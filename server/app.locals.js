const express = require('express');
const path = require('path');
module.exports = {
    publicDir: process.cwd() + "/server/public/",
    viewsDir: process.cwd() + "/server/views/",
    libDir: process.cwd() + "/server/lib",
    uploadsDir: process.cwd() + "/uploads/",
}